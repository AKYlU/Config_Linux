# é para tira o 5s
/etc/default/grub
GRUB_TIMEOUT=0

# para salva o grub
sudo update-grub

# é para ir para bio
systemctl reboot --firmware-setup

sudo visudo

akil ALL=(ALL) NOPASSWD: ALL

install p7zip-full
install playerctl
install steam
install firefox
install sway
install flathub
install code
install gnome-boxes
