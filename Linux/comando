# comando padra
ls
ls -a
top
free -h
cd ~
cd ..
mkdir
lsblk
cfdisk /dev
which

#comando
ncdu

# é para tira o 5s
/etc/default/grub
GRUB_TIMEOUT=0

# para salva o grub
sudo update-grub

# é para ir para bio
systemctl reboot --firmware-setup

sudo visudo

usuario ALL=(ALL) NOPASSWD: ALL

install p7zip-full playerctl steam firefox sway flathub gnome-boxes gamemode alacritty  sudo xbps-install -S ttf-opensans gamescope
install -t pattern devel_basis java-17-openjdk-devel libopenssl-devel ctags ncurses-devel silversearcher-ag fontconfig ImageMagick ImageMagick-devel software-properties-common git curl

sudo rm -r /etc/<nome_do_pacote>
rm -r ~/.config/<nome_do_pacote>

kernel
uname -a
cd /boot
Descomprimir el archivo
tar -xvf linux-6.7.1.tar.xz
cd linux-6.7.1

# Copiar configuración actual
cp /boot/config-$(uname -r) .config

Carregar os Módulos Atuais
make localmodconfig

# O iniciar desde cero
make menuconfig

Compilar el kernel
make
# make -j$(nproc)

Instalar los módulos del kernel
sudo make modules_install

Instalar el kernel
sudo make install

Actualizar el cargador de arranque (GRUB)
sudo update-grub

Reiniciar
sudo reboot

flatpak update

x11 apricativo
login
install xinit
install xorg fonts

drack ou claro
lxappearance

TOP &     # Deixa em segundo
PS        # Mostra quase tudo
FG TOP    # Primeiro plano

W TCH -N1 'FREE -H'/_M   (Incompleto com TOP)

PWD      # Vê onde o consultor

WHEREIS FIREFOX  # Fazendo caminho do FIREFOX

Copia comando Já juro tty  
. BASH_ALIASES  
SOURCE ~/.bash_aliases

Como saber o nome [ilegível]

(MAN)  
CLEAR    # Limpa  

FIND . -NAME nome do comando   # Acha arquivo

DISCO

DF -H

wlr-randr --output Virtual-1 --mode 1920x1080
