sudo yast

sudo apt-get remove “nome do programa sem aspas” --auto-remove

atualizar opensuse
sudo zypper refresh
sudo zypper dup
atualizar flathub
sudo flatpak update
flatpak update
flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo

sudo zypper removerepo
sudo zypper addrepo --refresh https://download.opensuse.org/repositories/system:/snappy/openSUSE_Tumbleweed/ snappy
sudo zypper --gpg-auto-import-keys refresh
sudo zypper install snapd
sudo systemctl enable --now snapd
sudo snap install core
sudo systemctl start snapd.apparmor
flatpak list
flatpak uninstall
sudo zypper clean --all 
rm -rf
mkdir
alsamixer
mkdir -p ~/.config/i3
touch ~/.config/i3/config
pav
hardware
RADV_PERFTEST
Tipo de arquivo	Comando
Zip	# sudo apt-get install unzip
Rar	# sudo apt-get install unrar-free    --  ou  -- #sudo apt-get install unrar
Tar	Já disponível em 11 entre 10 distribuições do Linux
Tar.gz	Já disponível em 11 entre 10 distribuições do Linux
Bz2	Já disponível em 11 entre 10 distribuições do Linux
Tar.bz2	Já disponível em 11 entre 10 distribuições do Linux
nano ~/.config/i3/config
sudo zypper remove --clean-deps
nano ~/.config/qtile/config.py
sudo zypper install zip
sudo zypper install curl

sudo rpm --import https://brave-browser-rpm-release.s3.brave.com/brave-core.asc

sudo zypper addrepo https://brave-browser-rpm-release.s3.brave.com/brave-browser.repo

sudo zypper install brave-browser

for_window [class=".*"] border none

Tipo de arquivo	Comando
Zip	# gunzip arquivo.zip
Rar	# unrar x arquivo.rar
Tar	# tar -xvf arquivo.tar
Tar.gz	# tar -vzxf arquivo.tar.gz
Bz2	# bunzip arquivo.bz2    -- ou -- # bunzip2 arquivo.bz2
Tar.bz2	# tar -jxvf arquivo.tar.bz2

# kernel
sudo zypper install gcc libelf-devel flex bison libopenssl-devel bc libelf-devel

sudo make modules_install install

sudo zypper rm --clean-deps yast2\*
sudo rm -r /etc/<nome_do_pacote>
rm -r ~/.config/<nome_do_pacote>

 sudo grub2-mkconfig -o /boot/grub2/grub.cfg

Defina o alvo padrão para gráfico
systemctl set-default graphical
sudo systemctl set-default graphical.target

# playsound
sudo zypper in gstreamer-devel python3-gst python3-devel gcc cairo-devel gobject-introspection-devel python3-playsound alsa-firmware 




